import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.astrositebuilder.app',
  appName: 'Astro Site Builder',
  webDir: 'dist',
};

export default config;
